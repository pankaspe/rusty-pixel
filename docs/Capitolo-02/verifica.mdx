---
sidebar_position: 9
title: "2.9 Verifica"
---
import Quiz from '@site/src/components/Quiz';

In questo bel capitolo, vediamo se le fondamente sono state costruite in maniera solida.  

***

<Quiz
  questions={[
    {
      text: "#1 Che cosa sono le variabili?",
      answers: [
        "Le variabili non sono importanti per la matematica o la programmazione",
        "Sono oggetti fisici che cambiano di posizione nel tempo", 
        "Sono costanti fisse che non cambiano mai.", 
        "Sono simboli che rappresentano dati variabili in matematica e programmazione, consentendo manipolazioni e calcoli", 
      ],
      correctAnswer: "Sono simboli che rappresentano dati variabili in matematica e programmazione, consentendo manipolazioni e calcoli",
    },  
    {
      text: "#2 Come vengono definite le variabili in Rust di default?",
      answers: [
        "Di default sono mutabili, il che significa che possono essere modificate liberamente senza restrizioni",
        "Di default sono immutabili, quindi non possono essere riassegnate una volta che il loro valore è stato assegnato", 
        "Di default sono globali, il che significa che sono accessibili da qualsiasi parte del programma senza restrizioni", 
        "Di default sono costanti, il che significa che non possono essere modificate dopo l'assegnazione del valore iniziale", 
      ],
      correctAnswer: "Di default sono immutabili, quindi non possono essere riassegnate una volta che il loro valore è stato assegnato",
    },  
    {
      text: "#3 Qual'è la differenza tra variabili shadowed e variabili mutabili?",
      answers: [
        "Variabili shadowed: variabili modificabili liberamente; variabili mutabili: variabili in Rust con mut per modificare i valori",
        "Le variabili shadowed nascondono variabili originali nel codice, mentre le variabili mutabili sono modificabili in JavaScript", 
        "Le variabili shadowed sono variabili locali con lo stesso nome di variabili globali, creando uno scope locale", 
        "Le variabili shadowed possono cambiare tipo; le variabili mutabili hanno valori fissi, non modificabili", 
      ],
      correctAnswer: "Le variabili shadowed sono variabili locali con lo stesso nome di variabili globali, creando uno scope locale",
    },
    {
      text: "#4 Cosa sono i tipi di dato in Rust?",
      answers: [
        "I tipi di dato definiscono il tipo di valore che una variabile può contenere, come numeri interi, decimali, booleani o testo",
        "Si riferiscono solo a numeri interi e decimali, escludendo booleani e testo", 
        "I tipi di dato sono fissi e non possono essere modificati una volta dichiarati", 
        "Rust non supporta tipi di dato, ogni variabile può contenere qualsiasi tipo di valore senza restrizioni", 
      ],
      correctAnswer: "I tipi di dato definiscono il tipo di valore che una variabile può contenere, come numeri interi, decimali, booleani o testo",
    },   
    {
      text: "#5 Cosa rappresenta il tipo di dato Char?",
      answers: [
        "Rappresenta una sequenza di parole o frasi all'interno di un programma, usato per mostrare messaggi all'utente",
        "Rappresenta un singolo carattere alfanumerico, come una lettera, un numero o un simbolo, e occupa 2 byte di memoria", 
        "Il tipo di dato Char rappresenta un numero intero in un range specifico, solitamente da -128 a 127", 
        "Char rappresenta una variabile che può contenere solo valori booleani, cioè vero o falso, indicando se una condizione è soddisfatta o meno", 
      ],
      correctAnswer: "Rappresenta un singolo carattere alfanumerico, come una lettera, un numero o un simbolo, e occupa 2 byte di memoria",
    },
    {
      text: "#6 Che differenza c'è tra una Tupla e un Array in Rust?",
      answers: [
        "Un array in Rust può avere una lunghezza variabile, mentre una tupla ha una lunghezza fissa e non può essere modificata dopo la sua creazione",
        "Le tuple in Rust vengono utilizzate per dati statici e immutabili, mentre gli array sono più adatti per dati dinamici che possono cambiare durante l'esecuzione del programma", 
        "A differenza degli array, le tuple in Rust possono essere utilizzate come chiavi nei dizionari, mentre gli array non possono essere utilizzati in questo modo", 
        "Una tupla in Rust può contenere elementi di diversi tipi di dati, ad esempio un intero e una stringa, mentre un array contiene elementi tutti dello stesso tipo di dato, come una serie di interi o stringhe", 
      ],
      correctAnswer: "Una tupla in Rust può contenere elementi di diversi tipi di dati, ad esempio un intero e una stringa, mentre un array contiene elementi tutti dello stesso tipo di dato, come una serie di interi o stringhe",
    },     
  ]}
/>

